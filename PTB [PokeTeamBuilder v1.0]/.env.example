# ============================================================
# PTB — Pokémon Team Builder v1.0
# Environment Configuration Template
# ============================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.
# ============================================================

# ── Flask Security ────────────────────────────────────────────
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
PTB_SECRET_KEY=change-me-to-a-random-secret-key

# ── Admin Panel ───────────────────────────────────────────────
# PIN for accessing the admin panel (desktop GUI)
# Use a strong numeric or alphanumeric PIN
ADMIN_PIN=change-me-to-a-secure-pin

# ── Server Configuration ──────────────────────────────────────
HOST=0.0.0.0
PORT=5000
DEBUG=False

# ── Email / SMTP (optional — for email verification) ─────────
# For Gmail: Use App Password (https://myaccount.google.com/apppasswords)
# For other providers: Use regular SMTP credentials
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SENDER_EMAIL=your-email@gmail.com
SENDER_PASSWORD=your-app-password-here

# Base URL for email verification links
BASE_URL=http://localhost:5000

# ── Database ──────────────────────────────────────────────────
DATABASE_PATH=data/social_community.db

# ── Memory Card Storage ───────────────────────────────────────
# Directory where .ptbmc files are stored
# Default: ~/.ptb/memory_cards/
# MEMORY_CARD_DIR=/path/to/memory_cards

# ── Feature Flags ─────────────────────────────────────────────
# Set to "true" to enable experimental features
ENABLE_MULTIPLAYER=true
ENABLE_TOURNAMENTS=true
ENABLE_SOCIAL_HUB=true

# ── Production Notes ──────────────────────────────────────────
# For production deployment:
# 1. Set DEBUG=False
# 2. Use a strong PTB_SECRET_KEY (32+ random bytes)
# 3. Set a secure ADMIN_PIN
# 4. Configure SMTP for email verification
# 5. Use gunicorn: gunicorn -w 4 -b 0.0.0.0:5000 web/app:app
# 6. Put behind a reverse proxy (nginx/caddy) with HTTPS
